services:
  interactive:
    build: 
      context: .
      dockerfile: ./Dockerfile
    restart: no
    stdin_open: true # docker run -i
    tty: true        # docker run -t
    shm_size: '64gb'
    working_dir: /workspace/plantsegstudio/
    volumes:
      # mount current directory, so changes in the host are reflected in the container
      - type: bind
        source: "."
        target: "/workspace/plantsegstudio"
      # might change based on host os; disable when this gives errors
      - type: bind
        source: "/tmp/.X11-unix"
        target: "/tmp/.X11-unix"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    # user: "containerUser"
    network_mode: host
    # env_file:
    #   - .env
    environment:
      - TZ=Europe/Amsterdam
      # environment variables to enable using display
      # might change based on host os; disable when this gives errors 
      - DISPLAY=${DISPLAY}
      - QT_X11_NO_MITSHM=1
